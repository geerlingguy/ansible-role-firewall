[Unit]
Description=Firewall
After=syslog.target network.target

[Service]
Type=oneshot
ExecStart=/etc/firewall.bash
ExecStop={{ firewall_flush_commands | join (' ; ') }}
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
